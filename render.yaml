services:
  - type: web
    name: sendpic-face-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python railway_main.py
    envVars:
      - key: PORT
        value: 8000
      - key: PYTHON_VERSION
        value: 3.11
